# Windows-only tool. The .cmd launchers in particular must arrive with CRLF
# endings, or cmd.exe can mis-parse them when someone downloads the ZIP.
* text=auto
*.cmd text eol=crlf
*.ps1 text eol=crlf
*.json text eol=lf
*.js   text eol=lf
*.md   text eol=lf
*.yml  text eol=lf
